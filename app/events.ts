import { createEvent } from "@/lib/custom-event.utils";

export const FocusMainSearch = createEvent("focus-main-search")

export const NewTopTab = createEvent("new-top-tab")

export const SaveCurFetcher = createEvent("save-cur-fetcher")
